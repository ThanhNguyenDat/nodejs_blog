<div class="mt-4">
    <div class="row">
        
        <div class="col-lg-3">
            <button>Get</button>
        </div>

        <div class="col-lg-3">
            <h2>{{book.name}}</h2>
            <iframe width="560" height="315" src="{{book.image}}" ></iframe>
            <a href="/books/{{book.slug}}">{{book.name}}</a>
            <p>{{book.description}}</p>

        </div>
        
    </div>
</div>